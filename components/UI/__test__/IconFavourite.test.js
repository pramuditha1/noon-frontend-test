import React from "react";
import { render } from "@testing-library/react";
import IconFavourite from "../IconFavourite";

describe("IconFavourite component", () => {
    test("should render without errors", () => {
        render(<IconFavourite/>);
    })
})