import React from "react";
import { render } from "@testing-library/react";
import IconHome from "../IconHome";

describe("IconHome component", () => {
  test("renders without errors", () => {
    render(<IconHome />);
  });
});
